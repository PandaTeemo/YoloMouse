#include <Yolomouse/Share/Root.hpp>

namespace Yolomouse
{
    //------------------------------------------------------------------------
    void LogShared( const Char* format, ... )
    {
        //SharedState::Instance().GetLog();
        // macros
        //SharedState::Instance().GetLog().Write
    }
}
